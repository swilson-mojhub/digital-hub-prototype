machine:
  environment:
    NOKOGIRI_USE_SYSTEM_LIBRARIES: true # speeds up installation of html-proofer

dependencies:
  post:
    - gulp build

test:
  override:
    - echo "test"

# test:
  # post:
    # - bundle exec htmlproofer ./_site --allow-hash-href --check-html --disable-external

# deployment:
#   prod:
#     branch: master
#     commands:
#       - rsync -va --delete ./_site/ circleci@stevenalanwilson.com:/var/www/html
